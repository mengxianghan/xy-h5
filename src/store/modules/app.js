const state = {
    complete: false // 初始化完成
}

const getters = {
    complete: state => state.complete
}

const mutations = {}

const actions = {}

export default {
    namespace: true,
    state,
    getters,
    mutations,
    actions
}
